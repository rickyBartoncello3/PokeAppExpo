import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  root: {
    alignItems: 'center',
  },

  amountLabel: {
    opacity: 0.65,
  },

  amount: {
    letterSpacing: -1,
  },
});

export default styles;
