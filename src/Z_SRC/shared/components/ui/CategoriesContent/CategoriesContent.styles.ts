import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  content: {
    flex: 1,
    gap: 8,
    width: '100%',
  },

  categoryList: {
    gap: 12,
  },

  categoryRow: {
    gap: 12,
  },
});
