import {IconName} from '@/src/Z_SRC/shared/constants/iconNames';

export interface TabBarIconProps {
  name: IconName;
  size?: number;
  color?: string;
  strokeWidth?: number;
  fillColor?: string;
}
